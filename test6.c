#include<stdio.h>
#include<string.h>
#define num 10

/*
        This program counts down from 10 to 0
        /*
         and has a nested multiline comment
        */

*/
int main() {
        int i = num;
        while(i >= 0) {
                printf("%d ", i);
                i--;
        }

        string ch = "calvin";
        printf("%d", strlen(ch));
        return 0;
}
